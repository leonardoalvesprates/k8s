namespace: labfleethelm
targetCustomizations:
- name: web
  clusterSelector:
    matchLabels:
      env: lab
  helm:
    values:
      replicas: 2
  dependsOn:
    - name: configmap

- name: configmap
  clusterSelector:
    matchLabels:
      env: lab
  yaml: 
    - configmap.yaml